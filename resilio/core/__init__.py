"""Deterministic repositories, calculations, and application services.

Core owns provider-neutral use-case logic and never imports the API or CLI
presentation layers. Focused packages expose their genuine public interfaces
through their own ``__init__`` modules.
"""
